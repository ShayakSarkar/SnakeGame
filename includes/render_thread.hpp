#include <bits/stdc++.h>

#ifndef RENDER_THREAD_ACTION_DEF
#define RENDER_THREAD_ACTION 1
void render_thread_action(snake& snk,food& fd,WINDOW* win,std::queue<int>& key_buffer,int& should_terminate);
#endif
